# 99. 評価ルーブリック

## 総合評価

| 評定 | 条件 |
|---|---|
| **A** | 全 Step 通過 / タグ運用が妥当 / **戻り先判定が 1 回以上発生して正しく対処** / 引き継ぎ JSON が 3 Phase 揃う / E2E シナリオが全パス |
| **B** | 完了するが `[QUESTION]` が浅い（数値未確認のまま実装） / タグ無し推測が 1〜2 件 / 戻り先判定なしで完走 |
| **C** | Step 0-A をスキップ / Phase 分割なし / 検証コマンド実行結果なし / `[BREAKING]` を見落として完了宣言 |

## Step 別チェックリスト

### Step 0-A（5 点）

- [ ] Q1 上限値・窓幅 を `[QUESTION]` で確認した（1）
- [ ] Q2 対象 API を `[QUESTION]` で確認した（1）
- [ ] Q3-4 応答仕様 / ヘッダ を `[QUESTION]` で確認した（1）
- [ ] Q5 識別キーを `[AMBIGUITY]` で挙げた（1）
- [ ] What / Done / Constraints / Non-Goals が構造化されている（1）

### Step 0-C（2 点）

- [ ] 「IaC は変更しない」が前提に含まれる（1）
- [ ] fakeredis 採用が前提に含まれる（1）

### Step 1（4 点）

- [ ] grep 結果が引用されている（1）
- [ ] `/orders` ゲートウェイ既存制限を発見し `[BREAKING]` 候補としている（2）
- [ ] 代替案が 2 つ以上、トレードオフ比較あり（1）

### Step 2（4 点）

- [ ] Phase が 3 つ程度に分かれ、各 300 行以内（1）
- [ ] 各 Phase に完了条件 3 種（機能 / テスト / 検証コマンド）がある（2）
- [ ] ロールバック手順が各 Phase に 1 行ある（1）

### Step 3 / Step 4 ×3（9 点 / 各 Phase 3 点）

- [ ] テストが Red → Green の順で書かれている（1）
- [ ] 検証コマンド実行結果が引き継ぎに貼られている（1）
- [ ] 引き継ぎ JSON が形式通り（1）

### Step 5-A（4 点）

- [ ] E2E シナリオ最低 3 つを実行し結果を貼る（2）
- [ ] `[BREAKING]` の後続対応が明記されている（1）
- [ ] 残存 `[OUT-OF-SCOPE]` の処理方針あり（1）

## 加点ボーナス（最大 +3）

- 戻り先判定が **複数 Step** で発生して全部正しく対処（+1）
- `[ASSUMPTION]` の検証を Step 5-A の E2E で実行した（+1）
- middleware 順序問題を Step 2 で予防できた（仕込みを回避）（+1）

## 失格条件

- `solution/` を盗み見した
- タグ無しの推測を 3 件以上含む
- 既存 `/users` / `/health` テストを壊したまま完了宣言した

## ふりかえり質問（受講者向け）

1. 一番効いた Step はどれか？
2. 戻り先判定が発生したのは Phase いくつ？ どの戻り先だった？
3. 依頼書の薄さに対して、何個の `[QUESTION]` を返した？
4. もし `solution/` 通りに進めなかった部分があれば、その判断根拠は？
